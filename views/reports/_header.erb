<div style="text-align:center; margin-bottom:12px">
  <div style="font-weight:800; font-size:1.1rem">Dalubhasaan ng Lungsod ng Lucena</div>
  <div style="font-weight:600; font-size:0.95rem; color:var(--muted)">Report on <%= @report_title || '—' %></div>
  <div style="font-size:0.85rem; color:var(--muted)">As of <%= @report_as_of || Date.today %> — Filter: <%= @report_filter_label || 'All dates' %></div>
  <hr style="margin-top:10px; border-color:rgba(255,255,255,0.04)">
</div>
